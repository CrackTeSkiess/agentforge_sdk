[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentforge"
version = "0.1.0"
description = "AgentForge Python SDK - Build and deploy RL agents for competitive multiplayer games"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.9"
authors = [
    {name = "AgentForge Team", email = "support@agentforge.io"}
]
maintainers = [
    {name = "AgentForge Team", email = "support@agentforge.io"}
]
keywords = [
    "reinforcement-learning",
    "rl",
    "games",
    "competition",
    "agents",
    "ai",
    "chess",
    "multiplayer",
    "bot",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Games/Entertainment :: Board Games",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]

dependencies = [
    "httpx>=0.27.0,<1.0.0",
    "websockets>=13.0,<15.0",
]

[project.optional-dependencies]
chess = ["chess>=1.10.0,<2.0.0"]
dev = [
    "pytest>=7.0,<9.0",
    "pytest-asyncio>=0.21.0,<1.0.0",
    "ruff>=0.6.0,<1.0.0",
    "mypy>=1.0,<2.0",
    "chess>=1.10.0,<2.0.0",
]
security = [
    "bandit>=1.7.0",
    "safety>=3.0.0",
]

[project.urls]
Homepage = "https://agentforge.io"
Documentation = "https://docs.agentforge.io"
Repository = "https://github.com/agentforge/agentforge_sdk.git"
Issues = "https://github.com/agentforge/agentforge_sdk/issues"
Changelog = "https://github.com/agentforge/agentforge_sdk/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["."]
include = ["agentforge*"]
exclude = ["tests*", "*test*", "main.py"]

[tool.setuptools.package-data]
agentforge = ["py.typed"]

[tool.ruff]
target-version = "py39"
line-length = 100
src = ["agentforge"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "S"]
ignore = ["B008"]

[tool.ruff.lint.isort]
known-first-party = ["agentforge"]

[tool.mypy]
python_version = "3.9"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
strict = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"